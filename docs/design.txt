Design principles

1. Make things modular whenever possible. If possible make code
   reusable by making a separate library to reuse the code.
2. DON't hard code any config value in the code. Write all the 
   modules with configurable parameters. Read this parameters from 
   separate config files.
3. Put fail check and throw exception wherever required.
